
export default function RecordPage ({param}){
  const validIdOfRecord:Boolean = (id:string)=>{
    if (id!==null && /^[a-z_]+$/.test(id)) {
      
    }
  }
  const router = useRouter();
  const id = param.id;
  
  
}