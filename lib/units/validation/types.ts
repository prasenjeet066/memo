class VDType {
  constructor (){
    
  }
  /**
   * @param [id] = string
   */
  static validIdOfRecords (id: string): boolean{
    var checkExist;
    if (id) {
      checkExist = null
    }
  }
  public isExistInIndex (val:string|number, rowName:string): object{
    if (rowName &&  val) {
      
    }
  }
}
VDType.validIdOfRecords()